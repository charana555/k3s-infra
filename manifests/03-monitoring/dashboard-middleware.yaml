apiVersion: v1
kind: Secret
metadata:
  name: dashboard-auth-secret
  namespace: kubernetes-dashboard
type: Opaque
stringData:
  users: "admin:$apr1$.yQ0vZQU$KY/HBLm0YtOWmgpSP3vgJ/"
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-auth
  namespace: kubernetes-dashboard
spec:
  basicAuth:
    secret: dashboard-auth-secret
---
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: dashboard-transport
  namespace: kubernetes-dashboard
spec:
  insecureSkipVerify: true
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: kubernetes-dashboard
spec:
  redirectScheme:
    scheme: https
    permanent: true
